{
  "name": "cocofresh-platform",
  "version": "1.0.0",
  "description": "Root package.json to bypass DigitalOcean component detection",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix backend"
  },
  "engines": {
    "node": "22.x"
  }
}
